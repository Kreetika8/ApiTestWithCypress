{
  "name": "gorestoapi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:run": "cypress run",
    "merge:reports": "npx mochawesome-merge cypress/reports/*.json > cypress/reports/report.json",
    "generate:report": "npx marge cypress/reports/report.json --reportDir cypress/reports/html",
    "test:report": "npm run cypress:run && npm run merge:reports && npm run generate:report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@faker-js/faker": "^9.9.0",
    "@types/node": "^24.0.14",
    "cypress": "^14.5.2",
    "cypress-mochawesome-reporter": "^4.0.0",
    "dotenv": "^17.2.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0",
    "typescript": "^5.8.3"
  }
}
